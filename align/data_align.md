# 数据对齐
传统的对齐，char字节是一字节对齐，short2字节对齐，int 4字节对齐
，double是8字节对齐的。
对齐的要求源自于CPU的要求，比如32位的CPU,那么数据对齐就是4字节。每次通过总线访问4个字节是快速的。
```c++
struct A {
  char a1;
  int a2;
  double a3;
};// 不对齐的话认为这就是13个字节，但是因为对齐的要求
```
